import { VistaRolesDirective } from './vista-roles.directive';

describe('VistaRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new VistaRolesDirective();
    expect(directive).toBeTruthy();
  });
});
